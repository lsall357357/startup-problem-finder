interface:
  display_name: "Startup Problem Finder"
  short_description: "Find early-stage startup and pitch-deck blockers"
  default_prompt: "Use $startup-problem-finder to review my early-stage startup and locate the problems investors may challenge."

policy:
  allow_implicit_invocation: true
